


module.exports = class notificationsController{

  static all(req, res){
    //fetches all notifications for a user
  }

  static clear(){
    //clears all notifications for a user
  }

  static read(){
    //marks one or many or all unread as read for a user
  }

  static delete(){
    //deletes one notification for a user
  }
}
